the .cvbi from vectras vm containing the rom-data.json,
thumbnail.png and .qcow2 (32 GB) is inside the zip which is given in the releases. do whatever you want with it ig, has sudo, firefox, git and curl.
credentials:

VM: alpine-vm

User: alpine-vm (i think? I forgot)

Password: ruben sim

Sudo password: ruben sim (if this doesn't work, try ribi)

OS: Alpine Linux x86_64

(you should probably inspect the rom-data.json first)
